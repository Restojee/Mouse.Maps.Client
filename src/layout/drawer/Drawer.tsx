import { StyledDrawer } from "@/layout/drawer/styles/StyledDrawer";

type Props = {
    activeTab: string
}
export const Drawer = (props: Props) => {
    return (
        <StyledDrawer>

        </StyledDrawer>
    )
}