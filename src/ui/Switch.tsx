import React, { ReactElement } from "react";

type Props = {
    switchKey: string;
    children: ReactElement[];
}
export const Switch = (props: Props) => {

    const { switchKey, children } = props;

    return null
}